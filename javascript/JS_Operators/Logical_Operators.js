// Logical AND (&&)
let x = true;
let y = false;
console.log(x && y); // false

// Logical OR (||)
let a = true;
let b = false;
console.log(a || b); // true

// Logical NOT (!)
let c = true;
console.log(!c); // false
