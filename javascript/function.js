// Functions

function addNumbers(num1, num2) {
    return num1 + num2;
}

// console.log(addNumbers(45, 50));

// Arrow functions

const subtractNumbers = (num1, num2) => {
    return num1 - num2
}
// console.log(subtractNumbers(45, 50));

const multiplyNumbers = (num1, num2) => num1 * num2;

console.log(multiplyNumbers(45, 50));